<source>
  @type forward
  bind 0.0.0.0
  port 24224
</source>

<match SmartCarDriverInfo>
  @type kafka
  brokers kafka:9092
  
  <format>
    @type json
  </format>

  default_topic driver-info

  required_acks -1
  # compression_codec gzip
</match>